{% extends "EkynaAdminBundle:Entity:sided.html.twig" %}

{% block title %}
    <h1>{{ 'ekyna_media.gallery.header.show'|trans|raw }}</h1>
{% endblock %}

{% block actions %}
    {{ admin_resource_btn(gallery, 'list') }}
    {{ admin_resource_btn(gallery, 'new') }}
{% endblock %}

{% block main %}
    <div class="actions">
        {{ admin_resource_btn(gallery, 'edit') }}
        {{ admin_resource_btn(gallery, 'remove') }}
    </div>
    <div class="show show-with-tabs">
        <ul class="nav nav-tabs">
            <li class="active"><a href="#media-gallery-general" data-toggle="tab">{{ 'ekyna_core.field.general'|trans }}</a></li>
        </ul>
        <div class="show-body">
            <div class="tab-content">
                <div class="tab-pane active" id="media-gallery-general">
                    {{ show_row(gallery.title, 'text', 'ekyna_core.field.title') }}
                    {{ show_row(path('ekyna_media_gallery_admin_tinymce', identifiers|merge({'galleryId': gallery.id})), 'tinymce', 'ekyna_core.field.description') }}
                    {{ show_row(gallery.medias, 'medias', 'ekyna_core.field.images') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}